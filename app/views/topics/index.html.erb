<div class="">
  <h1>Breakout</h1>
</div>

<% if user_signed_in? %>
  <%= form_for @topic do |f| %>
    <%= f.text_field "topic", class: "form-control flat input-hg", placeholder: "Add Topic" %>
  <% end %>
  <input class="form-control flat input-hg" type="text" placeholder="Add Topic"></input>
<% else %>
  <%= link_to "Sign Up", new_user_registration_path %>
  <%= link_to "Log in", new_user_session_path %>
<% end %>